let mongoose;_f07‍.x([["default",()=>_f07‍.o]]);_f07‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const VideoSchema = new Schema({
  title: String,
  release: String,
  age: String,
  runtime: String,
  characters: [String],
  director: String,
  summary: String,
  tags: [String],
  user: {
    _id: mongoose.Types.ObjectId,
    username: String,
  },
});

const Video = mongoose.model('Video', VideoSchema);
_f07‍.d(Video);

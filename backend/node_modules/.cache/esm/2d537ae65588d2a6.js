let mongoose;_558‍.x([["default",()=>_558‍.o]]);_558‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const ReviewSchema = new Schema({
  videoId: Number,
  text: String,
  publishedDate: {
    type: Date,
    default: Date.now,
  },
});

const Review = mongoose.model('Review', ReviewSchema);
_558‍.d(Review);
